import { Config } from "./src/config";

export const defaultConfig: Config = {
  url: "https://soulandsky.shop",
  match: "https://soulandsky.shop",
  maxPagesToCrawl: 50,
  outputFileName: "output.json",
  maxTokens: 2000000,
};
